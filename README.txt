1) frist you must download it:
$git clone https://github.com/albert2102/loopback3--task.git 

2) seconde you must install dependencies:
$npm install

3) thired run app:
$npm start  (or) $node . (this in production mode)
$npm run dev (in devolp mode)

4) you can test app run the command:
$mocha --timeout 2500  (note you can change timeout value)